import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    borderColor: '#e0e0e0',
    borderWidth: 1,
    borderRadius: 8,
    height: 50,
    backgroundColor: '#e0e0e0',
    margin: 2,
    marginBottom: 10,
  },
});
